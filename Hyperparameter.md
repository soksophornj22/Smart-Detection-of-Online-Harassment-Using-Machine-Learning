" The model used max_iter = 1000, L2 regularization, and the ‘lbfgs’ solver (or ‘liblinear’ for smaller datasets). Hyperparameter tuning was performed using GridSearchCV to find the best combination for optimal performance.

"
